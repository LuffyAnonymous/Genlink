.env
.env.backup
.venv/
__pycache__/
*.pyc
instance/
*.db
.postgres-data/
.DS_Store
*.log

# Standalone link-generation service: contains a hardcoded proxy
# credential and account-automation logic that shouldn't go to GitHub.
linkgen_service/

# Old project archive - contains a real API key, hardcoded proxy
# credential, and unrelated business spreadsheets. Never commit this.
random.zip

# Chelsea/Spurs add-on source archive - already extracted and
# integrated into linkgen_service/ and app/services/manual_link.py.
Tixlinx_Chelsea_Spurs_New_File.zip
.playwright-mcp/

# Another full project archive (with its own real .env, git history) -
# same reasoning as random.zip above. Never commit this.
Genlink.zip
